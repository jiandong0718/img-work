# 图片归档与生产状态管理系统 PRD

## 1. 产品概述

本产品用于将用户提供的 Excel 表格中的嵌入图片自动识别、清洗并归档，形成以“主图”为核心的数据列表。每张主图作为一条业务数据，支持状态维护、生产数量维护、套图上传、详情查看、筛选搜索、下载和操作记录。

系统的核心价值是把原本分散在 Excel、图片文件夹和人工沟通中的图片处理流程，沉淀为可追踪、可筛选、可维护的生产工作台。

## 2. 目标用户

- 图片整理/录入人员：负责上传 Excel、确认识别结果、完成归档。
- 出图人员：基于主图上传套图，维护出图进度。
- 验收人员：查看主图和套图，修改状态、填写备注。
- 生产跟进人员：维护生产数量和生产状态。

当前阶段需要账号密码登录，但暂不做复杂角色权限。系统通过登录用户自动记录操作人，避免用户在每个操作页面重复手填姓名。

## 3. 业务目标

- 支持读取 Excel 单元格内嵌的 jpg/png 图片。
- 支持表头不固定时的字段识别和导入确认。
- 自动跳过含“删除、返单”等排除字样的数据。
- 按用户自定义年月日进行归档。
- 每张图片形成一条主图记录，并支持状态维护。
- 支持图片中心统一管理已归档主图，并在主图详情页管理 10 张以内的套图。
- 支持按编号、名称、图片类型、状态、日期等条件搜索筛选。
- 支持图片下载。
- 支持导入报告和操作记录，方便追溯。
- 支持账号密码登录，关键操作自动绑定当前登录用户。

## 4. MVP 范围

### 4.1 包含

- Excel 上传。
- 导入前选择归档日期。
- 自动识别内嵌图片和编号。
- 自动识别并跳过删除、返单类数据。
- 重复编号跳过。
- 导入预览和确认入库。
- 图片归档入口页。
- 图片中心列表页。
- 手动上传图片。
- 主图状态修改。
- 主图生产数量维护。
- 主图详情页。
- 套图上传、删除、预览、下载。
- 编号和名称搜索。
- 状态、日期筛选。
- 登录用户自动记录。
- 操作日志。

### 4.2 暂不包含

- 复杂角色权限。
- 严格状态流转限制。
- 套图固定位置命名。
- 图片 AI 审核。
- 图片批量压缩规则配置。
- 复杂统计报表。
- Excel 导出。

## 5. 关键业务规则

### 5.1 Excel 导入规则

- 支持格式：`.xlsx` 优先，后续可视需要支持 `.xls`。
- 图片来源：单元格内嵌图片。
- 图片格式：jpg、png。
- 表头不固定，但必须能识别到图片和编号。
- 如果系统无法自动确定字段，需要进入字段确认步骤。
- 正式入库前必须展示导入预览。

### 5.2 跳过规则

一行数据满足以下任一条件时，不入库：

- 无图片。
- 无编号。
- 编号已存在。
- 图片格式不是 jpg/png。
- 行内任意关键字段出现排除字样，例如：删除、返单、不做、不要、作废。

跳过的数据需要进入导入报告，并说明跳过原因。

### 5.3 编号规则

- 编号为唯一标识。
- 导入时若发现重复编号，直接跳过新数据。
- MVP 阶段不覆盖旧数据。
- 后续可扩展为“覆盖、跳过、另存为新版本”三种策略。

### 5.4 归档规则

- 图片归档支持两种入口：Excel 导入、手动上传。
- 用户上传 Excel 或手动上传图片时需要选择归档日期。
- 归档日期支持年月日。
- 归档结构以日期为主，例如：`2026-06-06`。
- 同一次导入生成一个导入批次 ID。
- 手动上传可生成手动批次 ID，便于回溯。
- 图片中心中可同时按归档日期、导入/上传批次和图片类型筛选。

### 5.4.1 图片类型规则

- MVP 阶段支持两种图片类型：手提包、单肩背包。
- Excel 导入时可设置默认图片类型，也可在识别预览中逐行调整。
- 手动上传时可设置默认图片类型，也可逐张调整。
- 图片中心支持按图片类型筛选。
- 主图详情页支持维护图片类型。

### 5.5 状态规则

状态由用户自由修改，系统不限制状态流转顺序。

建议内置状态：

- 已入库
- 待核对
- 待出图
- 出图中
- 待验收
- 待生产
- 生产中
- 已完成
- 需修改
- 已修改
- 已删除

“已删除”表示系统内软删除，默认从常规列表隐藏，但可以通过筛选查看。

### 5.6 套图规则

- 每张主图最多上传 10 张以内套图。
- MVP 阶段套图按上传顺序展示。
- 套图支持上传、删除、预览、下载。
- 删除套图需要记录操作人和时间。

### 5.7 下载规则

- 主图支持单张下载。
- 套图支持单张下载。
- 详情页可下载当前主图及其套图。
- Web 端无法直接强制指定用户本地保存位置，实际保存位置由浏览器下载设置决定。
- 如果未来做桌面端，可支持选择本地保存目录。

### 5.8 登录与用户规则

- 未登录用户访问系统页面时，跳转到登录页。
- MVP 阶段支持账号 + 密码登录。
- 登录成功后默认进入“图片归档”页面。
- 用户退出登录后回到登录页。
- 用户状态为停用时，不允许登录。
- 上传 Excel、手动上传、修改状态、上传套图、删除套图、下载图片等关键操作，均自动记录当前登录用户。
- MVP 阶段只区分管理员和普通成员，暂不限制页面和按钮权限。

## 6. 用户流程

### 6.1 导入流程

1. 用户进入“导入 Excel”页面。
2. 用户选择归档日期。
3. 用户上传 Excel。
4. 系统解析表头、编号和内嵌图片。
5. 系统识别排除字样和重复编号。
6. 系统展示导入预览。
7. 用户确认入库。
8. 系统生成主图记录、导入批次和导入报告。
9. 用户进入图片中心查看数据。

### 6.1.1 手动上传流程

1. 用户进入“图片归档”页面。
2. 用户选择“手动上传”。
3. 用户选择归档日期和默认图片类型。
4. 用户批量上传多张图片，或单张上传图片。
5. 用户逐张填写编号、名称、图片类型和数量。
6. 系统校验编号是否重复。
7. 用户确认保存到归档。
8. 系统生成主图记录、手动上传批次和操作日志。
9. 用户进入图片中心查看数据。

### 6.2 状态维护流程

1. 用户在图片中心找到主图。
2. 用户选择目标状态。
3. 系统自动读取当前登录用户作为操作人。
4. 系统保存状态变更。
5. 系统写入操作日志。

### 6.3 套图管理流程

1. 用户点击主图进入详情页。
2. 用户查看主图和基础信息。
3. 用户上传套图。
4. 系统按上传顺序展示套图。
5. 用户可删除、预览或下载套图。
6. 所有操作写入日志。

## 7. 页面需求

### 7.0 登录页

核心组件：

- 系统名称。
- 账号输入框。
- 密码输入框。
- 登录按钮。
- 登录失败提示。

状态：

- 未输入。
- 登录中。
- 登录失败。
- 登录成功，进入图片归档。

### 7.1 图片归档页面

核心组件：

- Excel 导入入口。
- 手动上传入口。
- 最近归档批次提示。

说明：

- 图片归档页不直接展示完整图片列表。
- Excel 导入和手动上传最终都会生成图片中心里的主图记录。

### 7.2 导入 Excel 页面

核心组件：

- 归档日期选择器。
- 默认图片类型选择器。
- Excel 上传区。
- 排除关键词提示。
- 解析进度。
- 最近导入批次入口。

状态：

- 未上传。
- 上传中。
- 解析中。
- 解析失败。
- 解析成功，进入预览。

### 7.3 导入预览页面

核心组件：

- 成功入库预估数量。
- 跳过数量。
- 重复编号数量。
- 排除关键词跳过数量。
- 缺少图片/编号数量。
- 可入库数据预览表。
- 跳过数据明细。
- 确认入库按钮。

预览表字段：

- 缩略图。
- 编号。
- 名称。
- 图片类型。
- 识别状态。
- 跳过原因。
- 备注。

### 7.4 手动上传页面

核心组件：

- 归档日期选择器。
- 默认图片类型选择器。
- 批量上传入口。
- 单张上传入口。
- 待归档图片预览。
- 逐张编号、名称、图片类型、数量维护。
- 重复编号提示。
- 保存到归档按钮。

状态：

- 未上传。
- 上传中。
- 待补全信息。
- 编号重复。
- 可保存。
- 保存成功。

### 7.5 图片中心页面

核心组件：

- 日期筛选。
- 状态筛选。
- 图片类型筛选。
- 搜索框。
- 批次筛选。
- 仅看缺套图。
- 仅看未完成。
- 列表表格。
- 批量状态修改。

列表字段：

- 主图缩略图。
- 编号。
- 名称。
- 图片类型。
- 归档日期。
- 当前状态。
- 生产数量。
- 套图数量。
- 操作人。
- 更新时间。
- 操作按钮。

### 7.6 主图详情页

核心组件：

- 主图大图预览。
- 基础信息编辑区。
- 状态切换区。
- 生产数量维护。
- 套图上传和列表。
- 操作日志。

字段：

- 编号。
- 名称。
- 图片类型。
- 当前状态。
- 归档日期。
- 需求数量。
- 已生产数量。
- 备注。
- 当前登录用户。

### 7.7 状态看板页

核心组件：

- 按状态分组的数量概览。
- 各状态下的主图卡片。
- 快速进入详情。

MVP 可选，但适合出图和生产跟进场景。

## 8. 数据模型建议

### 8.1 主图记录 ImageItem

| 字段 | 说明 |
| --- | --- |
| id | 系统 ID |
| code | 编号，唯一 |
| name | 名称 |
| archiveDate | 归档日期 |
| mainImageUrl | 主图地址 |
| thumbnailUrl | 缩略图地址 |
| status | 当前状态 |
| type | 图片类型，例如手提包、单肩背包 |
| requiredQuantity | 需求数量 |
| producedQuantity | 已生产数量 |
| remark | 备注 |
| batchId | 导入批次 ID |
| lastOperatorUserId | 最近操作用户 ID |
| operatorName | 最近操作人 |
| createdAt | 创建时间 |
| updatedAt | 更新时间 |
| deletedAt | 软删除时间 |

### 8.2 套图记录 ImageAttachment

| 字段 | 说明 |
| --- | --- |
| id | 系统 ID |
| imageItemId | 主图 ID |
| fileUrl | 图片地址 |
| thumbnailUrl | 缩略图地址 |
| sortOrder | 上传顺序 |
| fileName | 文件名 |
| operatorUserId | 上传用户 ID |
| operatorName | 上传人 |
| createdAt | 上传时间 |
| deletedAt | 软删除时间 |

### 8.3 导入批次 ImportBatch

| 字段 | 说明 |
| --- | --- |
| id | 批次 ID |
| fileName | Excel 文件名 |
| archiveDate | 归档日期 |
| totalRows | 总行数 |
| importedCount | 成功入库数 |
| skippedCount | 跳过数 |
| duplicateCount | 重复编号数 |
| excludedCount | 排除关键词数 |
| failedCount | 解析失败数 |
| operatorUserId | 导入用户 ID |
| operatorName | 导入人 |
| createdAt | 创建时间 |

### 8.3.1 手动上传批次 ManualUploadBatch

| 字段 | 说明 |
| --- | --- |
| id | 批次 ID |
| archiveDate | 归档日期 |
| defaultType | 默认图片类型 |
| totalFiles | 上传文件数 |
| importedCount | 成功入库数 |
| duplicateCount | 重复编号数 |
| failedCount | 失败数 |
| operatorUserId | 上传用户 ID |
| operatorName | 上传人 |
| createdAt | 创建时间 |

### 8.4 用户 User

| 字段 | 说明 |
| --- | --- |
| id | 用户 ID |
| account | 登录账号，唯一 |
| displayName | 用户姓名/昵称 |
| passwordHash | 密码哈希 |
| role | 角色，MVP 预留管理员/普通成员 |
| status | 启用/停用 |
| lastLoginAt | 最近登录时间 |
| createdAt | 创建时间 |
| updatedAt | 更新时间 |

### 8.5 操作日志 OperationLog

| 字段 | 说明 |
| --- | --- |
| id | 日志 ID |
| imageItemId | 主图 ID |
| actionType | 操作类型 |
| beforeValue | 操作前 |
| afterValue | 操作后 |
| operatorUserId | 操作用户 ID |
| operatorName | 操作人 |
| remark | 备注 |
| createdAt | 操作时间 |

## 9. 非功能需求

### 9.1 性能

- 图片中心使用缩略图，避免直接加载原图。
- 超过 50 条数据建议分页或虚拟列表。
- Excel 解析过程需要显示进度。
- 大文件上传需要有失败重试提示。

### 9.2 可用性

- 导入前必须让用户确认归档日期。
- 跳过数据必须给出原因。
- 删除操作必须二次确认。
- 状态修改需要即时反馈。
- 图片加载失败时展示明确占位和错误提示。
- 登录失效时需要提示用户重新登录。

### 9.3 可追溯性

- 导入批次可追踪。
- 状态变更可追踪。
- 套图上传和删除可追踪。
- 数量修改可追踪。
- 所有关键操作可追踪到具体登录用户。

## 10. 验收标准

- 上传包含内嵌图片的 Excel 后，系统能识别 jpg/png 图片和编号。
- 选择归档日期后，数据能按该日期入库。
- 含“删除、返单”等字样的数据不会入库。
- 重复编号不会入库，并能在导入报告中看到原因。
- 入库后的每张主图都能在列表中看到。
- 图片中心能按编号、名称、图片类型、日期、状态筛选。
- 手动上传能批量上传或单张上传图片，并保存到图片中心。
- 点击主图能进入详情页。
- 详情页能上传、删除、预览和下载套图。
- 用户能自由修改主图状态。
- 状态、数量、套图相关操作能生成日志。
- 未登录用户不能进入工作台。
- 登录后关键操作能自动记录当前登录用户。

## 11. 后续扩展

- 角色和权限。
- 自定义状态字典。
- 自定义排除关键词。
- Excel 导出。
- 批量图片打包下载。
- 桌面端选择本地保存目录。
- 套图固定位置和类型管理。
- 数据统计看板。
- AI 图片质量检测。
- 返单原因结构化分析。
