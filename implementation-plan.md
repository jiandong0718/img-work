# 图片归档工作台 MVP 实施计划

## 1. 目标

本计划用于把 PRD 和技术设计拆成可开发、可验收、可排期的 MVP 任务。MVP 目标是先完成一条完整闭环：

登录 → 选择归档方式 → Excel 导入或手动上传 → 进入图片中心 → 查看详情 → 修改状态/数量 → 上传套图 → 记录日志。

## 2. 阶段拆分

### 阶段 1：项目基础与登录

目标：建立系统基础框架，保证用户必须登录后才能访问工作台。

任务：

- 初始化前后端项目结构。
- 建立用户表 `users`。
- 初始化一个管理员账号。
- 实现账号密码登录。
- 实现退出登录。
- 实现当前用户接口。
- 实现未登录访问拦截。
- 前端增加登录页。
- 前端左侧显示当前登录用户。

验收标准：

- 未登录访问工作台时进入登录页。
- 输入正确账号密码后进入图片归档。
- 登录后能看到当前用户。
- 点击退出登录后回到登录页。
- 停用用户不能登录。

## 3. 阶段 2：数据模型与文件存储

目标：完成主图、套图、批次、日志的基础存储能力。

任务：

- 建立 `image_items` 主图表。
- 建立 `image_attachments` 套图表。
- 建立 `import_batches` Excel 导入批次表。
- 建立 `manual_upload_batches` 手动上传批次表。
- 建立 `operation_logs` 操作日志表。
- 实现图片文件保存。
- 实现缩略图生成。
- 实现软删除字段。
- 实现编号唯一索引。

验收标准：

- 主图编号不能重复。
- jpg/png 图片能保存到文件存储。
- 上传后能生成缩略图。
- 删除主图时不物理删除数据。
- 操作日志能记录用户、动作和时间。

## 4. 阶段 3：图片归档入口页

目标：完成“图片归档”入口页，只负责提供两种归档方式。

任务：

- 前端实现图片归档页面。
- 增加入口 1：Excel 导入。
- 增加入口 2：手动上传。
- 点击入口进入对应操作页。
- 左侧菜单保留图片归档、图片中心、状态看板。

验收标准：

- 图片归档页面不展示完整列表。
- 页面能清楚看到 Excel 导入和手动上传两个入口。
- 点击 Excel 导入进入 Excel 导入页。
- 点击手动上传进入手动上传页。

## 5. 阶段 4：图片中心

目标：完成已归档图片的统一列表管理。

任务：

- 实现图片中心列表接口。
- 支持分页。
- 支持编号/名称搜索。
- 支持归档日期筛选。
- 支持状态筛选。
- 支持图片类型筛选。
- 支持批次筛选。
- 支持套图数量展示。
- 支持批量修改状态。
- 支持进入主图详情。

验收标准：

- Excel 导入和手动上传的数据都能在图片中心看到。
- 能按手提包、单肩背包筛选。
- 能搜索编号和名称。
- 能进入主图详情。
- 批量改状态会写入操作日志。

## 6. 阶段 5：手动上传

目标：支持没有 Excel 的图片归档方式。

任务：

- 实现单张图片上传。
- 实现批量图片上传。
- 上传前选择归档日期。
- 上传前选择默认图片类型。
- 支持逐张填写编号。
- 支持逐张填写名称。
- 支持逐张修改图片类型。
- 支持填写需求数量。
- 保存前校验编号重复。
- 保存成功后生成手动上传批次。
- 保存成功后写入操作日志。

验收标准：

- 可以一次上传多张 jpg/png 图片。
- 可以只上传一张图片。
- 编号为空不能保存。
- 编号重复的数据不能入库。
- 保存成功后能在图片中心看到。
- 操作人自动记录为当前登录用户。

## 7. 阶段 6：Excel 导入

目标：完成从 Excel 内嵌图片中批量生成主图记录。

任务：

- 上传 Excel 文件。
- 读取 Excel 单元格内嵌图片。
- 识别图片所在行。
- 智能识别编号列。
- 智能识别名称列。
- 支持默认图片类型。
- 支持逐行调整图片类型。
- 扫描删除、返单、不做、不要、作废等排除字样。
- 校验重复编号。
- 展示导入预览。
- 展示跳过原因。
- 确认入库。
- 生成导入批次。
- 写入操作日志。

验收标准：

- 能读取 Excel 内嵌 jpg/png 图片。
- 有图片和编号的数据可入库。
- 命中删除/返单等字样的数据不入库。
- 重复编号不入库。
- 导入预览能展示入库、跳过、重复、异常。
- 确认入库后能在图片中心看到数据。

## 8. 阶段 7：主图详情与套图

目标：完成单张主图的完整维护能力。

任务：

- 展示主图大图。
- 展示编号、名称、图片类型、归档日期。
- 修改状态。
- 修改需求数量。
- 修改已生产数量。
- 上传套图。
- 删除套图。
- 套图按上传顺序展示。
- 限制套图最多 10 张。
- 下载主图。
- 下载套图。
- 展示操作日志。

验收标准：

- 从图片中心点击详情进入主图详情。
- 修改状态后图片中心同步更新。
- 上传套图后套图数量更新。
- 删除套图需要二次确认。
- 超过 10 张套图不能继续上传。
- 所有操作写入日志。

## 9. 阶段 8：状态看板

目标：按状态查看工作进度。

任务：

- 按状态聚合主图数量。
- 展示已入库、待出图、待验收、待生产、需修改等列。
- 卡片展示主图缩略图、编号、类型、数量、套图数。
- 点击卡片进入详情。

验收标准：

- 状态数量与图片中心一致。
- 状态修改后看板同步变化。
- 点击卡片能进入详情。

## 10. 阶段 9：下载与日志

目标：补齐追溯和下载能力。

任务：

- 主图单张下载。
- 套图单张下载。
- 主图 + 套图打包下载，后续可选。
- 导入报告下载。
- 操作日志列表。
- 按用户、动作、时间筛选日志。

验收标准：

- 用户能下载主图。
- 用户能下载套图。
- 下载操作写入日志。
- 导入报告能说明成功、跳过、重复、异常数量。

## 11. 推荐开发顺序

1. 登录与用户。
2. 数据表与文件存储。
3. 图片中心列表。
4. 手动上传。
5. 主图详情与套图。
6. Excel 导入。
7. 状态看板。
8. 下载与操作日志。

原因：

- 手动上传比 Excel 解析更简单，适合作为第一条入库闭环。
- 图片中心和详情先做出来，Excel 导入完成后可以直接复用展示和维护能力。
- Excel 内嵌图片解析风险最高，应在基础业务闭环稳定后处理。

## 12. MVP 完成标准

- 用户必须登录才能进入系统。
- 用户能通过手动上传创建主图。
- 用户能通过 Excel 导入创建主图。
- 所有主图都进入图片中心。
- 图片中心能按编号、名称、类型、状态、日期筛选。
- 用户能进入详情维护状态、数量和套图。
- 操作日志能追踪到当前登录用户。
- 删除、重复编号、排除关键词等关键规则生效。
